# Matching Engine

A correctness-first, single-instrument limit order book and matching engine.
Single-threaded, deterministic, in-memory, price-time priority.

**Status:** Phase 0 (spec and skeleton).
